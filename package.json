{
  "name": "kumanga",
  "version": "0.1.0",
  "private": true,
  "description": "Kumanga \u2014 a local-first, bring-your-own-key manga creation harness.",
  "workspaces": [
    "manga-studio"
  ],
  "overrides": {
    "sharp": "^0.35.3",
    "postcss": "^8.5.26",
    "next": {
      "postcss": "^8.5.26"
    }
  },
  "scripts": {
    "dev": "npm --prefix manga-studio run dev",
    "build": "npm --prefix manga-studio run build",
    "start": "npm --prefix manga-studio run start",
    "test": "npm --prefix manga-studio run test",
    "typecheck": "npm --prefix manga-studio run typecheck",
    "lint": "npm --prefix manga-studio run lint"
  }
}